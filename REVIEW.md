# Castor site review notes

## Verified working
- Build passes: `npm run build` succeeds, 404.html generated for GitHub Pages SPA routing
- All tests pass: 40 vitest tests green (`npm test`)
- Lint passes: `npm run lint` clean (eslint src cli)

## Findings from code review so far
- 37 files have uncommitted modifications (pre-existing working tree state, not mine)
- `hub`: reports free tier... (see previous note)

## Pending
- Awaiting user direction on next steps
