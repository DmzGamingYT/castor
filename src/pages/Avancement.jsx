import ProjectProgress from "../components/ProjectProgress.jsx";

/* Page dédiée à l'avancement du projet — la roadmap complète,
   accessible depuis l'accueil (aperçu) et le header. */
export default function Avancement() {
  return <ProjectProgress />;
}
